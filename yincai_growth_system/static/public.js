document.addEventListener("DOMContentLoaded",function(){
  const toggle=document.querySelector(".menu-toggle"),nav=document.querySelector(".main-nav");
  if(toggle&&nav){toggle.addEventListener("click",function(){const open=nav.classList.toggle("open");toggle.setAttribute("aria-expanded",String(open));document.body.classList.toggle("menu-open",open)});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");document.body.classList.remove("menu-open")}))}
  document.querySelectorAll("[data-language-select]").forEach(select=>select.addEventListener("change",()=>{window.location.assign(select.value)}));
  document.querySelectorAll("form[data-working]").forEach(form=>form.addEventListener("submit",function(){if(!form.checkValidity())return;const button=form.querySelector("button[type=submit],button:not([type])");if(button&&!button.disabled){button.dataset.label=button.textContent;button.textContent=form.dataset.working;button.classList.add("working")}}));
  if("IntersectionObserver" in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".solution-grid article,.process-grid article,.product-card,.video-frame").forEach(el=>observer.observe(el))}
  const analyticsId=document.body.dataset.analyticsId,banner=document.querySelector("[data-cookie-banner]");
  function loadAnalytics(){if(!analyticsId||!/^G-[A-Z0-9]+$/i.test(analyticsId)||window.gtag)return;const script=document.createElement("script");script.async=true;script.src="https://www.googletagmanager.com/gtag/js?id="+encodeURIComponent(analyticsId);document.head.appendChild(script);window.dataLayer=window.dataLayer||[];window.gtag=function(){window.dataLayer.push(arguments)};window.gtag("js",new Date());window.gtag("config",analyticsId,{anonymize_ip:true})}
  if(analyticsId&&banner){const choice=localStorage.getItem("yc_cookie_consent");if(choice==="accepted")loadAnalytics();else if(!choice)banner.hidden=false;banner.querySelector("[data-cookie-accept]").addEventListener("click",()=>{localStorage.setItem("yc_cookie_consent","accepted");banner.hidden=true;loadAnalytics()});banner.querySelector("[data-cookie-reject]").addEventListener("click",()=>{localStorage.setItem("yc_cookie_consent","essential");banner.hidden=true})}
});
